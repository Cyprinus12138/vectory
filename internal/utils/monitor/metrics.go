package monitor

// Define the meta information here
const (
	NameSpace = "vectory"
)

// Define metric(names) constant here
const (
	MetricCount     = "count"
	MetricLatency   = "latency"
	MetricErrorCode = "err"
)

// Define scenarios here
const (
	RpcReq  = "rpc"
	HttpReq = "http"
)
